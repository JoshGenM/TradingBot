---
description: Run the Friday weekly review manually. Uses local .env credentials. No git push.
---
Follow all steps in routines/weekly-review.md using local .env credentials.
Skip the git commit/push step — this is a local ad-hoc run.
