---
description: Run the pre-market research workflow manually. Uses local .env credentials. No git push.
---
Follow all steps in routines/pre-market.md using local .env credentials.
Skip the git commit/push step — this is a local ad-hoc run.
