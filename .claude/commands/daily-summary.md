---
description: Run the daily EOD summary workflow manually. Uses local .env credentials. No git push.
---
Follow all steps in routines/daily-summary.md using local .env credentials.
Skip the git commit/push step — this is a local ad-hoc run.
