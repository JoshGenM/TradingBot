---
description: Run the midday position scan manually. Uses local .env credentials. No git push.
---
Follow all steps in routines/midday.md using local .env credentials.
Skip the git commit/push step — this is a local ad-hoc run.
