---
description: Run the market-open trade execution workflow manually. Uses local .env credentials. No git push.
---
Follow all steps in routines/market-open.md using local .env credentials.
Skip the git commit/push step — this is a local ad-hoc run.
